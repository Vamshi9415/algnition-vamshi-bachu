# ML Engine

Forecasting, validation, budgeting, uncertainty, Gemini insight helpers, and the
project data/config/artifact folders live here.

The repo is organized into three top-level parts:

- `frontend/` for the React UI
- `backend/` for the FastAPI app
- `ml_engine/` for the forecasting pipeline
